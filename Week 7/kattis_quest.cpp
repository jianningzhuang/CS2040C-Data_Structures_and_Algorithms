#include <bits/stdc++.h>
using namespace std;

int main(){
  int n;
  cin >> n;
  for (int i = 0; i < n; i++){
    string command;
    cin >> command;
    if (command == "add"){
      int e, g;
      cin >> e >> g;
    }
    else if (command == "query"){
      int x;
      cin >> x;
    }
  }
}
